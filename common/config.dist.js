// Extension data source — template file.
// Copy this to config.js (gitignored) and adjust the URL if needed.
// The dev server (scripts/dev.js) does this automatically.
const GITHUB_PAGES_BASE = 'https://pedrotroller.github.io/statuswatch';
const DEBUG = false;
